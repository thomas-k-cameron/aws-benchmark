[package]
name = "aws-benchmark"
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    "./aws-benchmark-remove",
    "./aws-benchmark-common",
    "./aws-benchmark-setup",
]
exclude = ["./serde-aws-sdk"]

[build]
rustflags = ["--cfg", "aws_sdk_unstable"]